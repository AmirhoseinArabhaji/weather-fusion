POSTGRES_USER=weather
POSTGRES_PASSWORD=weather_secret
POSTGRES_DB=weather_fusion
POSTGRES_PORT=5432

REDIS_PASSWORD=redis_secret
REDIS_PORT=6379

BACKEND_PORT=8080
FRONTEND_PORT=3000

# set by CI on deploy (image tag = commit sha); leave unset for local `docker compose build`
IMAGE_TAG=latest
